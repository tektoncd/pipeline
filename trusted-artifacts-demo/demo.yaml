apiVersion: tekton.dev/v1
kind: PipelineRun
metadata:
  generateName: demo-
spec:
  workspaces:
    - name: shared
      persistentVolumeClaim:
        claimName: storage
  pipelineRef:
    name: demo