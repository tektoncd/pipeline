apiVersion: tekton.dev/v1alpha1
kind: TaskRun
metadata:
  generateName: unnamed-steps-
spec:
  taskSpec:
    steps:
    - image: ubuntu
      script: 'true'
    - image: ubuntu
      script: 'true'
    - image: ubuntu
      script: 'true'
