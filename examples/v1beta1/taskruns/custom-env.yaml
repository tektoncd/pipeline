apiVersion: tekton.dev/v1beta1
kind: TaskRun
metadata:
  generateName: custom-env-
spec:
  taskSpec:
    steps:
    - image: busybox
      script: |
        #!/usr/bin/env sh
        [[ $MY_VAR1 == foo ]]
      env:
      - name: MY_VAR1
        value: foo
